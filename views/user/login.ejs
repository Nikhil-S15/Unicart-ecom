<!-- Add the following line in the head section of your HTML file -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">

<!-- Use the following code wherever you want to display the eye icon -->

<div class="container">
    <main class="main">
      <section class="pt-150 pb-150">
        <div class="container">
          <div class="row">
            <div class="col-lg-10 m-auto">
              <div class="row">
                <div class="col-lg-5 mx-auto">
                  <div class="login_wrap widget-taber-content p-5 background-white border-radius-10 mb-md-5 mb-lg-0 mb-sm-5">
                    <div class="padding_eight_all bg-white">
                      <div class="heading_s1">
                        <h3 class="mb-30">Login</h3>
                        <p id="error" class="text-danger"></p>
                      </div>
                     
                     
                      <form id="login" 
                      action="/login"
                      method="post"
                      >
                        <div class="form-group">
                          <input type="text" required="" id="email" name="email" placeholder="Your Email">
                        </div>
                     

                        <span class="form-group">
                          <input required="" type="password" id="password" style="position: relative" name="password" placeholder="Password">
                          <i class='bi bi-eye-fill' onclick="change()" style="cursor: pointer; position: absolute;bottom:26.5%;right:39.5%"></i>
                        </span>
                        <div class="login_footer form-group">
                        </div>
                        <div class="form-group d-flex ">
                          
                          <button  class="btn btn-fill-out btn-block hover-up" >Log in</button>
                        </div>
                        <span> or</span>
                        <div class="text-center text-lg-start mt-1 ">
                          <div class="text-muted  mb-15"><a href="/otplogin" id="get-otpLogin">Login with OTP</a></div>
                            <p class="small fw-bold mt-2 pt-1 mb-0">Don't have an account? <a href="/signup"
                                class="link-danger">Register</a></p>
                          </div>
                         
                      </form>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
    </main>
  </div>

<style>
  #eye{
    background-color: aqua;
    margin-left: 21rem;
    width: 20px;
    margin-top: 100px;
  }
</style>
 
  
  <script>
    function change(){
      
      let pass = document.getElementById('password')
      pass.type == 'password'? pass.type = 'text': pass.type = 'password'
    }

    

function validate() {
    const email = document.getElementById('email')
    const password = document.getElementById('password')

    const emailValue = email.value.trim()
    const passwordValue = password.value.trim()

    $.ajax({
      url: '/login',
      method: 'post',
      data: {
        email: emailValue,
        password: passwordValue,
      },
      success(response) {
        console.log(response);
        // if (response.status) {
        //   location.href = '/'
        // } else if(response.blocked){
        //   document.getElementById('error').innerText = "You are blocked by admin"
        // } else {
        //   document.getElementById('error').innerText = "Email or Password is incorrect"

        // }
      
      }
    })
  }


  </script>

  <!--get otp login modal form-->
